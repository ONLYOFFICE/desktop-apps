; -- Desktop Editors --

; Uncomment the lines below to be able to compile the script from within the IDE.
; #define _ONLYOFFICE
; #define _ARCH "64"
; #define _ARCH "32"
; #define _WIN_XP

#define _UPDMODULE
#define SCRIPT_CUSTOM_FILES
#define UNINSTALL_USE_CLEAR_PAGE

#include "common.iss"
