{{#spritesheet}}
.pixel-ratio__1_5 {
    .tool-icon, .menu__icon {
        background-image: url({{{escaped_image}}});
        background-size: {{scaled width 1.5}}px auto;
    }
}
{{/spritesheet}}
{{#sprites}}
.tool-icon.{{name}} {
    background-position-y: {{scaled offset_y 1.5}}px;
}
{{/sprites}}

